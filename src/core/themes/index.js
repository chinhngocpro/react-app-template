import lightTheme from "./collections/light";

const Themes = {
    light: lightTheme,
}

export const themesConfig = {
    topbar: 'light',
    sidebar: 'light',
    layout: 'light',
    theme: 'light',
}

export default Themes;